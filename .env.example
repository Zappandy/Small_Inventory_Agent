MODAL_RECEIPT_ENDPOINT=https://your-modal-app.modal.run/extract

# Do not commit real values.
# Only needed for deploying Modal from your local machine or CI:
# generate via uv run modal token new --profile <WORKSPACE>
